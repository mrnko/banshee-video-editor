# External media tools

Development builds discover FFmpeg automatically. Release builds may bundle approved binaries here:

```text
tools/bin/ffmpeg.exe
tools/bin/ffprobe.exe
tools/bin/realesrgan-ncnn-vulkan.exe
tools/bin/models/*
```

Binary files are intentionally ignored by Git. Verify hashes and licenses before packaging.
